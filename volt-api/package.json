{
  "name": "volt-api",
  "version": "1.0.0",
  "private": true,
  "description": "Volt backend API (Fastify + Zod, in-memory store + Prisma schema as source of truth)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "e2e": "node scripts/e2e.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "fastify": "^5.0.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.9",
    "@types/node": "^20.12.0",
    "prisma": "^5.22.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
