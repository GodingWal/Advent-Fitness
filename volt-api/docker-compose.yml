services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: volt
      POSTGRES_USER: volt
      POSTGRES_PASSWORD: volt
    ports:
      - "5432:5432"
    volumes:
      - volt-pgdata:/var/lib/postgresql/data

volumes:
  volt-pgdata:
